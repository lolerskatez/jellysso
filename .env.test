# Test Environment Configuration
NODE_ENV=development
JELLYFIN_BASE_URL=http://localhost:8096
PORT=3000
SESSION_SECRET=test-session-secret-for-development
JWT_SECRET=test-jwt-secret-for-development
SHARED_SECRET=test-shared-secret-for-plugin
HTTPS_PORT=3443

# Database (for future use)
# DATABASE_URL=sqlite:///./test-data/companion.db

# OIDC Configuration
OIDC_ISSUER=http://localhost:3000
OIDC_CLIENT_ID=jellyfin-companion
OIDC_CLIENT_SECRET=companion-secret

# External OIDC Clients Configuration (JSON array)
# Example: [{"client_id": "keycloak-app", "client_secret": "keycloak-secret", "grant_types": ["authorization_code"], "redirect_uris": ["https://keycloak.example.com/callback"], "response_types": ["code"], "scope": "openid profile email"}]
EXTERNAL_OIDC_CLIENTS=[]