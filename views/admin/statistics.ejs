<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Statistics - Jellyfin Companion Admin">
  <title>Statistics - Jellyfin Companion</title>
  <link rel="stylesheet" href="/css/theme.css">
  <link rel="stylesheet" href="/css/layout.css">
  <link rel="stylesheet" href="/css/fontawesome.css">
  <script src="/js/chart.min.js"></script>
  <style>
    /* Time Range Selector */
    .time-range {
      display: flex;
      gap: var(--spacing-xs);
      border: 1px solid var(--border-color);
      border-radius: var(--border-radius);
      overflow: hidden;
    }
    .time-range-btn {
      padding: var(--spacing-sm) var(--spacing-lg);
      border: none;
      background: white;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--text-secondary);
      transition: all var(--transition);
    }
    .time-range-btn:not(:last-child) {
      border-right: 1px solid var(--border-color);
    }
    .time-range-btn:hover {
      background: var(--bg-secondary);
    }
    .time-range-btn.active {
      background: var(--primary);
      color: white;
    }

    /* Stats Grid */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: var(--spacing-lg);
      margin-bottom: var(--spacing-xl);
    }
    .stat-card {
      background: white;
      padding: var(--spacing-lg);
      border-radius: var(--border-radius);
      border: 1px solid var(--border-color);
      box-shadow: var(--shadow-sm);
      display: flex;
      align-items: center;
      gap: var(--spacing-md);
    }
    .stat-icon {
      width: 48px;
      height: 48px;
      border-radius: var(--border-radius);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
      flex-shrink: 0;
    }
    .stat-icon.primary { background: rgba(0, 102, 204, 0.1); color: var(--primary); }
    .stat-icon.success { background: rgba(16, 185, 129, 0.1); color: var(--success); }
    .stat-icon.warning { background: rgba(245, 158, 11, 0.1); color: var(--warning); }
    .stat-icon.danger { background: rgba(239, 68, 68, 0.1); color: var(--danger); }
    .stat-icon.purple { background: rgba(139, 92, 246, 0.1); color: #8b5cf6; }
    .stat-icon.cyan { background: rgba(6, 182, 212, 0.1); color: #06b6d4; }
    .stat-content h3 {
      margin: 0;
      font-size: 1.75rem;
      color: var(--gray-900);
      font-weight: 700;
    }
    .stat-content p {
      margin: 0;
      font-size: 0.8125rem;
      color: var(--text-secondary);
    }

    /* Charts Grid */
    .charts-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: var(--spacing-lg);
      margin-bottom: var(--spacing-lg);
    }
    .chart-card {
      background: white;
      padding: var(--spacing-lg);
      border-radius: var(--border-radius);
      border: 1px solid var(--border-color);
      box-shadow: var(--shadow-sm);
    }
    .chart-card h3 {
      margin: 0 0 var(--spacing-md) 0;
      font-size: 1rem;
      color: var(--gray-900);
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
    }
    .chart-card h3 i {
      color: var(--primary);
    }
    .chart-wrapper {
      position: relative;
      height: 250px;
    }
    .chart-card.full-width {
      grid-column: 1 / -1;
    }
    .chart-card.full-width .chart-wrapper {
      height: 300px;
    }

    /* Metrics Table */
    .table-card {
      background: white;
      border-radius: var(--border-radius);
      border: 1px solid var(--border-color);
      box-shadow: var(--shadow-sm);
      overflow: hidden;
    }
    .table-card h3 {
      margin: 0;
      padding: var(--spacing-lg);
      font-size: 1rem;
      color: var(--gray-900);
      border-bottom: 1px solid var(--border-color);
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
    }
    .table-card h3 i {
      color: var(--primary);
    }
    .metrics-table {
      width: 100%;
      border-collapse: collapse;
    }
    .metrics-table th {
      background: var(--bg-secondary);
      padding: var(--spacing-md);
      text-align: left;
      font-size: 0.8125rem;
      font-weight: 600;
      color: var(--gray-900);
      text-transform: uppercase;
    }
    .metrics-table td {
      padding: var(--spacing-md);
      border-bottom: 1px solid var(--border-color);
      font-size: 0.9375rem;
    }
    .metrics-table tr:last-child td {
      border-bottom: none;
    }
    .metrics-table tr:hover {
      background: var(--bg-secondary);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }
      .charts-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="app-container">
    <%- include('../partials/navigation', { user, currentPage: 'admin-statistics' }) %>
    <main class="main-content">
      <header class="header">
        <div class="header-left">
          <button class="mobile-menu-toggle" id="mobileMenuToggle" aria-label="Toggle menu">
            <i class="fas fa-bars"></i>
          </button>
          <h1 class="header-title">Statistics
          </h1>
        </div>
        <div class="header-right">
          <div class="time-range">
            <button class="time-range-btn active" data-range="24h">24h</button>
            <button class="time-range-btn" data-range="7d">7 days</button>
            <button class="time-range-btn" data-range="30d">30 days</button>
            <button class="time-range-btn" data-range="all">All</button>
          </div>
        </div>
      </header>

      <div class="page-content">
        <!-- Stats Cards -->
        <div class="stats-grid">
          <div class="stat-card">
            <div class="stat-icon primary">
              <i class="fas fa-exchange-alt"></i>
            </div>
            <div class="stat-content">
              <h3 id="totalRequests"><%= stats.totalRequests || 0 %></h3>
              <p>Total Requests</p>
            </div>
          </div>
          <div class="stat-card">
            <div class="stat-icon success">
              <i class="fas fa-check-circle"></i>
            </div>
            <div class="stat-content">
              <h3 id="successRate"><%= stats.successRate || 0 %>%</h3>
              <p>Success Rate</p>
            </div>
          </div>
          <div class="stat-card">
            <div class="stat-icon danger">
              <i class="fas fa-times-circle"></i>
            </div>
            <div class="stat-content">
              <h3 id="failedRequests"><%= stats.failedRequests || 0 %></h3>
              <p>Failed Requests</p>
            </div>
          </div>
          <div class="stat-card">
            <div class="stat-icon purple">
              <i class="fas fa-user-clock"></i>
            </div>
            <div class="stat-content">
              <h3 id="activeUsers"><%= stats.activeUsers || 0 %></h3>
              <p>Active Sessions</p>
            </div>
          </div>
          <div class="stat-card">
            <div class="stat-icon cyan">
              <i class="fas fa-users"></i>
            </div>
            <div class="stat-content">
              <h3 id="totalUsers"><%= stats.totalUsers || 0 %></h3>
              <p>Total Users</p>
            </div>
          </div>
          <div class="stat-card">
            <div class="stat-icon warning">
              <i class="fas fa-database"></i>
            </div>
            <div class="stat-content">
              <h3 id="dbSize"><%= stats.dbSize || '0 MB' %></h3>
              <p>Database Size</p>
            </div>
          </div>
        </div>

        <!-- Charts -->
        <div class="charts-grid">
          <div class="chart-card">
            <h3><i class="fas fa-tasks"></i> Actions Distribution</h3>
            <div class="chart-wrapper">
              <canvas id="actionChart"></canvas>
            </div>
          </div>
          <div class="chart-card">
            <h3><i class="fas fa-chart-pie"></i> Success vs Failure</h3>
            <div class="chart-wrapper">
              <canvas id="statusChart"></canvas>
            </div>
          </div>
          <div class="chart-card full-width">
            <h3><i class="fas fa-chart-area"></i> Activity Timeline</h3>
            <div class="chart-wrapper">
              <canvas id="timelineChart"></canvas>
            </div>
          </div>
        </div>

        <!-- Metrics Table -->
        <div class="table-card">
          <h3><i class="fas fa-list-alt"></i> Detailed Metrics</h3>
          <table class="metrics-table">
            <thead>
              <tr>
                <th>Metric</th>
                <th>Value</th>
              </tr>
            </thead>
            <tbody id="metricsTableBody">
              <tr>
                <td><i class="fas fa-sign-in-alt" style="color: var(--primary); margin-right: var(--spacing-sm);"></i> User Logins</td>
                <td><strong><%= stats.userLogins || 0 %></strong></td>
              </tr>
              <tr>
                <td><i class="fas fa-exclamation-triangle" style="color: var(--danger); margin-right: var(--spacing-sm);"></i> Failed Logins</td>
                <td><strong><%= stats.failedLogins || 0 %></strong></td>
              </tr>
              <tr>
                <td><i class="fas fa-user-plus" style="color: var(--success); margin-right: var(--spacing-sm);"></i> Users Created</td>
                <td><strong><%= stats.usersCreated || 0 %></strong></td>
              </tr>
              <tr>
                <td><i class="fas fa-cog" style="color: var(--warning); margin-right: var(--spacing-sm);"></i> Settings Changes</td>
                <td><strong><%= stats.settingsChanges || 0 %></strong></td>
              </tr>
              <tr>
                <td><i class="fas fa-plug" style="color: #8b5cf6; margin-right: var(--spacing-sm);"></i> API Calls</td>
                <td><strong><%= stats.apiCalls || 0 %></strong></td>
              </tr>
              <tr>
                <td><i class="fas fa-qrcode" style="color: #06b6d4; margin-right: var(--spacing-sm);"></i> QuickConnect Uses</td>
                <td><strong><%= stats.quickConnects || 0 %></strong></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </main>
  </div>

  <script src="/js/mobile-nav.js"></script>
  <script src="/js/admin-statistics.js"></script>
  <script src="/js/admin-dashboard.js"></script>
</body>
</html>
